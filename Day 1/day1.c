#include <stdio.h>

int main(int argc, char const *argv[])
{
    int const depth_readings[7];
    int data;

    FILE *fp = ("sonar_sweep", "r");
    fopen(fp);

    while(fp != NULL){
        
    }
    fscanf("%d", depth_readings[]);




    return 0;
}

